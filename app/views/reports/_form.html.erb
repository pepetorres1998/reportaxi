<%= form_with(model: report, local: true) do |form| %>
  <% if report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
        <% report.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :taximeter %>
    <%= form.number_field :taximeter %>
  </div>

  <div class="field">
    <%= form.label :driving %>
    <%= form.number_field :driving %>
  </div>

  <div class="field">
    <%= form.label :cleaning %>
    <%= form.number_field :cleaning %>
  </div>

  <div class="field">
    <%= form.label :service %>
    <%= form.number_field :service %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
